; Local PHP overrides (optional). Prefer starting server with:
; php -d upload_max_filesize=12M -d post_max_size=12M artisan serve --host=0.0.0.0 --port=8000
upload_max_filesize=12M
post_max_size=12M
memory_limit=256M
